# Core
runpod>=1.6.0
requests>=2.32.0
boto3>=1.34.0

# torch & torchaudio are pre-installed in the base Docker image

# Wan I2V
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece>=0.2.0
Pillow>=10.0.0

# Upscaling / face restoration
opencv-python-headless>=4.8.0

# Whisper
openai-whisper>=20231117

# AudioCraft dependencies (audiocraft itself installed with --no-deps)
av
encodec
einops
flashy
lameenc
num2words
