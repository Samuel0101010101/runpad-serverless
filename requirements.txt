# Core
runpod>=1.6.0
requests>=2.32.0
boto3>=1.34.0

# Pin torch ecosystem to match base image (runpod/pytorch cu1281-torch290)
# These are pre-installed but we pin to prevent deps from upgrading them
torch==2.9.0
torchaudio==2.9.0
torchvision==0.22.0

# Wan 2.2 TI2V (diffusers installed from git in Dockerfile for WanPipeline support)
transformers>=4.40.0,<4.50.0
accelerate>=0.30.0
sentencepiece>=0.2.0
Pillow>=10.0.0
ftfy

# Upscaling / face restoration
opencv-python-headless>=4.8.0
scipy

# Whisper
openai-whisper>=20231117

# AudioCraft dependencies (audiocraft itself installed with --no-deps)
av
encodec
einops
flashy
julius
lameenc
num2words
soundfile
xformers
spacy==3.7.6
hydra-core>=1.1
hydra_colorlog
demucs
librosa
torchmetrics
protobuf
