# Core
runpod>=1.6.0
requests>=2.32.0
boto3>=1.34.0

# torch, torchaudio, torchvision are pre-installed in the base Docker image
# Do NOT list them here; instead we use --no-deps for packages that would upgrade them

# Wan 2.2 TI2V (diffusers installed from git in Dockerfile for WanPipeline support)
transformers>=4.40.0,<4.50.0
accelerate>=0.30.0
sentencepiece>=0.2.0
Pillow>=10.0.0
ftfy

# Upscaling / face restoration
opencv-python-headless>=4.8.0
scipy

# Whisper deps (whisper itself installed with --no-deps in Dockerfile)
tiktoken
more-itertools
numba

# AudioCraft / demucs deps (audiocraft, demucs, xformers installed with --no-deps)
av
encodec
einops
flashy
julius
lameenc
num2words
soundfile
hydra-core>=1.1
hydra_colorlog
librosa
protobuf

# Face swap (InsightFace — installed with --no-deps in Dockerfile)
onnxruntime
